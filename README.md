# MedicalDiseaseDetection
A Java-based Medical Disease Detection System that analyzes user-input symptoms to identify possible diseases and provide basic treatment suggestions. The application uses core Java concepts and conditional logic to simulate a simple healthcare diagnostic system through a console-based interface. 
